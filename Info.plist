<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Bundle identity -->
    <key>CFBundleName</key>
    <string>Clew</string>
    <key>CFBundleDisplayName</key>
    <string>Clew</string>
    <key>CFBundleIdentifier</key>
    <string>io.clew.app</string>
    <key>CFBundleVersion</key>
    <string>1.0.0</string>
    <key>CFBundleShortVersionString</key>
    <string>1.0.0</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>

    <!-- Executable / icon -->
    <key>CFBundleExecutable</key>
    <string>Clew</string>
    <key>CFBundleIconFile</key>
    <string>logo.icns</string>

    <!-- macOS version requirement: 13.0 Ventura+
         (MLX 0.15+ requires macOS 13; PySide6 6.6+ requires 11) -->
    <key>LSMinimumSystemVersion</key>
    <string>13.0</string>

    <!-- Architecture: Apple Silicon preferred -->
    <key>LSArchitecture</key>
    <string>arm64</string>

    <!-- Category: Developer Tools -->
    <key>LSApplicationCategoryType</key>
    <string>public.app-category.developer-tools</string>

    <!-- High-DPI / Retina support -->
    <key>NSHighResolutionCapable</key>
    <true/>
    <key>NSPrincipalClass</key>
    <string>NSApplication</string>

    <!-- Document types: open .py, .md, .json, .txt files from Finder -->
    <key>CFBundleDocumentTypes</key>
    <array>
        <dict>
            <key>CFBundleTypeName</key>
            <string>Source Code</string>
            <key>CFBundleTypeRole</key>
            <string>Editor</string>
            <key>LSItemContentTypes</key>
            <array>
                <string>public.python-script</string>
                <string>public.javascript-source</string>
                <string>public.typescript-source</string>
                <string>public.shell-script</string>
                <string>public.source-code</string>
            </array>
        </dict>
        <dict>
            <key>CFBundleTypeName</key>
            <string>Markdown</string>
            <key>CFBundleTypeRole</key>
            <string>Editor</string>
            <key>LSItemContentTypes</key>
            <array>
                <string>net.daringfireball.markdown</string>
            </array>
        </dict>
        <dict>
            <key>CFBundleTypeName</key>
            <string>Plain Text</string>
            <key>CFBundleTypeRole</key>
            <string>Editor</string>
            <key>LSItemContentTypes</key>
            <array>
                <string>public.plain-text</string>
            </array>
        </dict>
    </array>

    <!-- Sandboxing: NOT enabled. Clew needs:
         - arbitrary file read/write (open any folder as project)
         - subprocess execution (terminal, LSP, model inference)
         - inbound/outbound network (HF Hub downloads, API providers)
         Sandbox would force TCC permission prompts on every file access
         and break the agent runtime. We run unsandboxed, like VS Code. -->
    <key>LSUIElement</key>
    <false/>

    <!-- Allow opening files from Finder double-click -->
    <key>NSDocumentRevisionsSupportMode</key>
    <string>0</string>

    <!-- Dark mode: tell macOS we handle it ourselves -->
    <key>NSRequiresAquaSystemAppearance</key>
    <false/>

    <!-- Disable smooth scrolling (PySide6 handles it) -->
    <key>NSScrollAnimationEnabled</key>
    <false/>

    <!-- URL schemes (future: clew:// URI for deep linking) -->
    <key>CFBundleURLTypes</key>
    <array>
        <dict>
            <key>CFBundleURLName</key>
            <string>io.clew.app.url</string>
            <key>CFBundleURLSchemes</key>
            <array>
                <string>clew</string>
            </array>
        </dict>
    </array>

    <!-- Copyright / human-readable -->
    <key>NSHumanReadableCopyright</key>
    <string>Copyright 2024 Clew Contributors. Apache License 2.0.</string>

    <!-- Crash reporting: disabled by default (no telemetry) -->
    <key>NSAppTransportSecurity</key>
    <dict>
        <key>NSAllowsArbitraryLoads</key>
        <true/>
    </dict>
</dict>
</plist>
